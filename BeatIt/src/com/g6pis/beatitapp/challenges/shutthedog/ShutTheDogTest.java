package com.g6pis.beatitapp.challenges.shutthedog;

/*import static org.junit.Assert.*;

import org.junit.Test;

public class ShutTheDogTest {

	
	@Test
	public void constructorTest() {
		ShutTheDog std = new ShutTheDog("4", "Shut The Dog", 2, 3);
		
		assertEquals("4",std.getChallengeId());
		assertEquals("Shut The Dog",std.getName());
		assertEquals(2,std.getLevel());
		assertEquals(3,std.getMaxAttempt());
		assertEquals(3, std.getLives());
		assertEquals(false, std.isHasWon());
		assertEquals(false, std.isCompleted());
		assertEquals(5, std.getArrayLength());
		
	}
	
	@Test
	public void finishedTest() {
		ShutTheDog std = new ShutTheDog("42", "Shut The Dog", 2, 3);
		int resultados[] = {100, 200, 300, 400, 500};
		std.setResults(resultados);
		std.setLives(2);
		
		std.finishChallenge();
		
		assertEquals(0,std.getLives());
		assertEquals(false,std.isHasWon());	
	}
	
	@SuppressWarnings("deprecation")
	@Test
	public void settersTest() {
		ShutTheDog std = new ShutTheDog("4", "Shut The Dog", 2, 3);
		std.setScore(500);
		std.setArrayLength(2);
		std.setLives(1);
		std.setChallengeId("5");
		std.setHasWon(true);
		std.setLevel(1);
		
		assertEquals(500,std.getScore());
		assertEquals(true,std.isHasWon());
		assertEquals(2, std.getArrayLength());
		assertEquals(1, std.getLives());
		assertEquals("5", std.getChallengeId());
		assertEquals(1, std.getLevel());
	}
	
	@SuppressWarnings("deprecation")
	@Test
	public void scoreTest() {
		ShutTheDog std = new ShutTheDog("42", "Shut The Dog", 2, 3);
		
		int resultados[] = {100, 200, 300, 400, 500};
		std.setResults(resultados);
		
		assertEquals(78.33, std.calculateScore());
		
	}

}*/
